// src/whatsapp.mjs — sending through the Meta Cloud API.
//
// Sends text and images to WHATSAPP_PHONE_NUMBER_ID with a System User token.
// Holds toWhatsApp(), which normalizes the model's output: Markdown `**bold**`
// becomes WhatsApp's single-asterisk `*bold*`. The prompt asks for it too, but
// formatting is deterministic, so code guarantees it.

// TODO: implement.
