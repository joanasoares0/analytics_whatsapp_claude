// scripts/smoke.mjs — sanity check: database + the four chart types.
//
// Queries both views and renders one chart of each type (donut,
// horizontal_bar, vertical_bar, gauge), printing the URLs. No model calls, so
// it is the cheapest way to tell whether the plumbing is alive.

// TODO: implement.
